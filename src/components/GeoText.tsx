import { Text } from "@react-three/drei";

const GeoText: React.FC = () => {
  return (
    <Text>
      <meshBasicMaterial color="red" />
      Hello World!!!
    </Text>
  );
};

export default GeoText;
